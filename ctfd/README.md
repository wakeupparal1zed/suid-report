# Врайтап

Найдите SUID-бинарь и выясните, что он запускает. Модифицируйте доступный скрипт так, чтобы флаг оказался в читаемом месте.

Пример (после редактирования скрипта и запуска SUID):

```bash
cat /home/ctf/flag.txt
```
