version: "3"

services:
  suid-report:
    build: .
    restart: unless-stopped
    network_mode: host
    environment:
      - FLAG=vsosh{testflag}
      - CTF_USER=ctf
      - CTF_PASS=ctf123
      - SSH_PORT=2223
